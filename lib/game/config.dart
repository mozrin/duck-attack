class GameConfig {
  static const double duckSpeed = 50.0;
  static const double duckFleeMultiplier = 2.0; // Twice as fast as waddle
  static const double duckEatDuration = 2.0; // Delay before flying off
}
